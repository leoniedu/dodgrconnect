% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/std_geometry.R
\name{std_geometry}
\alias{std_geometry}
\title{Convert Mixed Geometry Types to Lines or Points}
\usage{
std_geometry(m)
}
\arguments{
\item{m}{An sf object that may contain mixed geometry types}

\item{geom_out}{Character string specifying the desired output geometry type.
Must be either "POINT" or "LINESTRING". Default is "POINT".}
}
\value{
An sf object with:
\itemize{
\item Consistent geometry type (either all LINESTRING or all POINT)
\item All original attributes preserved
\item Empty columns removed
\item New sequential ID column added
}
}
\description{
Converts an sf object containing mixed geometry types into a consistent geometry type,
either LINESTRING or POINT. The function processes POLYGON and MULTIPOLYGON geometries
by converting them to the specified output geometry type while preserving other attributes.
}
\details{
The function performs the following steps:
\enumerate{
\item Identifies geometry types in the input sf object
\item Separates geometries that need conversion
\item Converts POLYGON/MULTIPOLYGON to specified geometry type
\item Combines converted geometries with original ones
\item Cleans up empty columns and adds sequential IDs
}
}
\examples{
\dontrun{
# Convert mixed geometries to points
points_sf <- m2poly(mixed_sf, geom_out = "POINT")

# Convert mixed geometries to lines
lines_sf <- m2poly(mixed_sf, geom_out = "LINESTRING")
}

}
